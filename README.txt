1. Возникла проблема с переадресацией чата из канала в чат с ботом(жмешь на кнопку и тебя отправляет в чат с ботом + отправляется сам аукцион). 
Как я почитал в документации, то нельзя одновременно присваивать url и call.data, call.data просто не обрабатывается.

2. WORD. Реализую документа я сделал, но мой PyCharm просто не дает скачать нужный документ, всегда выдает ошибку, почему — хотел бы разобраться вместе(код реализации функции написан).

3. Потоки. Не до конца разобрался с потоками, при запланированном отправлении сообщений через поток, возможно и скорее всего, они пересекаются, читал форумы и даже выловил проблему на Stack Overflow, не помогли :( Система работает, но иногда отправляется по 2-3 сообщений.
Аналогичная проблема с автоставками, реализация несложная, но из-за потоков непонятно как корректно обрабатывать ее.

Файлы бота - main.py, data_base_control
Файлы системы управления QT - main_window, users_logs, post_loading, history_logs, admin_system.

Надеюсь, что именно потоки разберем вместе, так как доделать до конца — хочу.
По остальным пунктам все корректно отрабатывает.
<3
